#include <iostream>
using namespace std;

int main()
{
    int number;
    int sum = 0;
    int counter = 0;
    while (counter < 4)
    {
        cin >> number;
        sum = number + sum;
        counter++;
    }
    cout << "sum is:" << sum << endl;
    return 0;
}
